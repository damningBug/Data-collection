<Window x:Class="Data_collection.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Data_collection.Views"
        mc:Ignorable="d"
        Title="数据库连接设置" Height="350" Width="400" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="服务器地址:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Server}" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="数据库名:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Database}" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="端口号:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Port}" Margin="5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="用户名:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Username}" Margin="5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="密码:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="4" Grid.Column="1" Margin="5" PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="5">
            <Button Content="测试连接" Command="{Binding TestConnectionCommand}" 
                    Width="80" Margin="5"/>
            <Button Content="确定" Command="{Binding SaveCommand}" 
                    Width="75" Margin="5"/>
            <Button Content="取消" Command="{Binding CancelCommand}" 
                    Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
